#include <stdio.h>
void main()
{
    // int - integer = whole number
    int a;  // declaration
    a = 10; // initialization
    printf("the value of a is %d\n", a);

    a = 20; // initialization second time

    printf("the value of a is %d", a);
}